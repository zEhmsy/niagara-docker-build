# The build context is the installer folder (often several GB): we only send the
# Niagara 5 Debian zip to the daemon, not the Windows MSI nor the RPM.
# If your zip is named differently, add its `!<pattern>` line here.
*
!Niagara_5_Debian-*.zip
!*Debian*.zip
!*debian*.zip
